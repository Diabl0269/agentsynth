# Docs-integrity ratchet baseline (scripts/check-docs.sh). ONE entry kind, STRICT ratchet:
#   naming <path>  -- a legacy docs/**/*.md file whose basename isn't lowercase kebab-case.
#                     Presence-only: fixed (renamed) means the entry is simply gone from a
#                     future --update. Never add a NEW entry -- rename the file instead.
# --allow-growth is the deliberate, reviewed exception. See check-docs.sh's own header comment
# for the full mechanism, and docs/development/docs-guard.md for why only naming is grandfathered -- every
# other check (B/C/D/E) is zero-tolerance and never baselined.
